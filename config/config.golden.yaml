# July 2026 golden-path runtime profile.
# Select with ANIMETTA_CONFIG=config/config.golden.yaml.

persona: "anima.v0.1"

services:
  asr: mock
  tts: alice_vc
  agent: deepseek
  local_llm: null
  vad: silero

system:
  host: "0.0.0.0"
  port: 12394
  debug: false
  log_level: "INFO"
  runtime_profile: golden
  golden_tts_timeout_seconds: 25.0
  long_term_memory_mode: "off"
  enable_tools: false
  enable_subtitle_translation: false
  enable_active_memes: false

humor:
  enabled: false
