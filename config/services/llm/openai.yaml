# OpenAI Agent 配置

memory_enabled: true
llm_config:
  type: openai
  model: "gpt-4o-mini"  # gpt-4o / gpt-4o-mini / gpt-4-turbo
  api_key: "${OPENAI_API_KEY}"
  temperature: 0.7  # 0.0(确定) - 2.0(随机)
  max_tokens: 4096