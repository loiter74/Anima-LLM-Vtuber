# Local LoRA LLM Configuration (Auto-generated)
llm_config:
  type: local_lora
  base_model_name: "${ANIMA_BASE_MODEL_PATH}"
  lora_path: "${ANIMA_LORA_PATH}"
  device: cpu  # 临时使用CPU（RTX 5090 sm_120架构PyTorch尚不支持）
  max_new_tokens: 512
  temperature: 0.8
  top_p: 0.9
