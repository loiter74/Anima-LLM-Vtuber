# 本地 LoRA 微调模型配置（自动生成）
llm_config:
  type: local_lora
  base_model_name: "${ANIMETTA_BASE_MODEL_PATH}"
  lora_path: "${ANIMETTA_LORA_PATH}"
  device: cuda  # 会自动降级到 CPU
  max_new_tokens: 512
  temperature: 0.8
  top_p: 0.9
