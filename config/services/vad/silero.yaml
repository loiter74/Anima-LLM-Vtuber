# Silero VAD 配置

type: silero
sample_rate: 16000
prob_threshold: 0.4        # 语音概率阈值
db_threshold: 60           # 分贝阈值
required_hits: 3           # 开始说话需要的连续命中次数 (约 0.1s)
required_misses: 24        # 停止说话需要的连续未命中次数 (约 0.8s)
smoothing_window: 5        # 平滑窗口大小