## 1. Test-first contracts

- [x] 1.1 Add failing DOM lifecycle tests for expanded mount, 1.4-second idempotent collapse, collapsed copy, and early cleanup.
- [x] 1.2 Add failing style assertions for tokenized 52% substrate opacity, full-opacity foreground content, 200 ms transition, and reduced-motion behavior.
- [x] 1.3 Replace the prior central-safe-area plugin assertion with failing adaptive top-gap and collapsed-state assertions.

## 2. Dynamic island implementation

- [x] 2.1 Measure the status-rail/danmaku free interval, publish notification-scoped placement variables, and recompute them on bounded geometry changes.
- [x] 2.2 Implement expanded and collapsed notification states with the approved dimensions, copy, semantic indicators, timer, and idempotent cleanup.
- [x] 2.3 Apply the translucent dynamic-island visual treatment using existing Animetta tokens and a non-animated reduced-motion presentation.
- [x] 2.4 Preserve the existing audio element, accessibility live region, report data, privacy, single-output OBS monitoring, and frozen live review fixtures.

## 3. Automated verification

- [x] 3.1 Run focused notification, lip-sync, and review-plugin Vitest suites through red and green phases.
- [x] 3.2 Run Python 3.13 preflight, frontend typecheck, full Vitest, production build, OpenSpec strict validation, `quality validate`, and `test-affected`.
- [x] 3.3 Confirm `text-boundaries` and `sparse` names, messages, and exact assertions remain unchanged.

## 4. Fresh OBS acceptance

- [x] 4.1 Capture a fresh Playwright/OBS `tts-failover` run proving the final island is collapsed, translucent, inside the measured top gap, and clear of both neighbors.
- [x] 4.2 Confirm existing billing-to-local backend, audio, performance, lip-sync, privacy, console/page/request, and Chrome/OBS stability assertions still pass.
- [x] 4.3 Obtain operator approval for expanded transition prominence and collapsed top-bar appearance.
- [x] 4.4 Run the Docker startup protocol through a sub-agent, proving Qwen 8766 and host 8767 identities remain unchanged and both Compose logs contain no Traceback/ERROR.
- [x] 4.5 Restore the original OBS scene, Browser Source settings, audio monitoring state, and temporary review resources after success, failure, or interruption.
