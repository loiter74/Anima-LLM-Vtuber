"""One-command hermetic demo of the Minecraft survival action chain.

Packages the typed fallback workflow with a
deterministic ``ScriptedBridge`` so the full plan → call tool → handle failure
→ recover → continue chain is demonstrable without a Minecraft server, the
Node.js bot, Docker, or an LLM.
"""
