# 发布工作流

## 发布前检查

发布之前先执行测试。测试必须在发布动作开始之前完成，因为未经测试的内容不能发布。

- 发布前必须通过测试。
- 如果测试没有通过，就不要发布。

## 推送目标

发布时使用主干作为唯一目标。不要把任务分支或个人分支作为发布目标。

- 只允许推送 main。
- 推送前再次确认当前目标是 main。

## 执行顺序

1. 运行测试并确认测试通过。
2. 确认推送目标为 main。
3. 测试已通过且目标确实是 main 后，执行推送。
