# ============================================================================
# Animetta -- Local AI Inference Dependencies
# ============================================================================
# Heavy packages for local model inference. Install on top of core:
#   pip install -r requirements-local-ai.txt
#
# Requires: CUDA-capable GPU, NVIDIA Container Toolkit (for Docker)
# ============================================================================

-r requirements.txt

# --- PyTorch ---
torch>=2.0.0
torchaudio>=2.0.0

# --- TTS: Kokoro (open-weight, 82M params) ---
# Kokoro 0.9.x is not installed in the supported Python 3.13 profile because
# its upstream metadata rejects Python 3.13. The contrib provider remains
# import-guarded until a compatible upstream release is available.

# --- TTS: Qwen3 (CustomVoice model) ---
qwen-tts>=0.1.1

# --- ASR: Faster-Whisper ---
faster-whisper>=1.0.0

# --- ASR: FunASR ---
funasr>=1.0.0
modelscope>=1.0.0
numba>=0.61  # Supported transitive constraint for funasr/umap-learn

# --- Local LLM (LocalLoraLLM) ---
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0

# --- Memory: Vector Search ---
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
chromadb>=0.4.0

# --- VAD: Silero ---
silero-vad>=5.0.0

# --- Audio Processing ---
pydub>=0.25.0

# --- Host RVC Singing Voice Conversion ---
faiss-cpu>=1.15.0
ffmpeg-python>=0.2.0
praat-parselmouth>=0.4.7
pyworld>=0.3.5
torchcrepe>=0.0.24

# --- Bilibili Live Danmaku ---
bilibili-api-python>=17.0.0

# --- Observability ---
langfuse>=4.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.30.0

# --- Redis (optional: multi-instance session sharing) ---
redis[hiredis]>=5.0
